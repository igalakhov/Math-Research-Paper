function [sorted numInversions] = mergeAndCountInversions(list1, list2)
  numInversions = 0;
  sorted = [];
  l1bound = 1;
  l2bound = 1;
  while (true)
    if((l1bound == length(list1)) && (l2bound == length(list2)))
      break
    elseif(list1(l1bound) < list2(l2bound)
    
    
    end
  end